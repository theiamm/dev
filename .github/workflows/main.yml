name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2   # Обновленная версия actions/checkout

    - name: Deploy to Server
      uses: appleboy/ssh-action@v0.1.9  # Используй конкретную версию, например, v0.4.2
      with:
        host: 164.90.214.44
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: 22
        script: |
          cd /root/django_deploy_app/myproject
          git pull origin main
          # Добавь другие команды, которые нужно выполнить на сервере после обновления кода


